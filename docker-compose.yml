version: "3.8"

services:
  backend:
    build:
      context: ./backend
    container_name: clearfix_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
